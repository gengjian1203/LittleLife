<!--pages/demo/Banner/Banner.wxml-->
<swiper
  indicator-dots="true"
  autoplay="true"
  interval="5000"
  circular="true"
>
  <block wx:for="{{list}}" wx:key="{{item.id}}">
    <swiper-item>
      <image src="{{item.url}}" mode="aspectFil"></image>
    </swiper-item>
  </block>
</swiper>